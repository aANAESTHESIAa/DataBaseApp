<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aanaesthesiaa.databaseapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataBaseApp">
        <activity android:name=".SearchActivity"></activity>
        <activity android:name=".AddItemActivity" />
        <activity android:name=".AddEmployeeActivity" />
        <activity android:name=".UpdDelEmployeeActivity" />
        <activity android:name=".UpdDelDepartmentActivity" />
        <activity android:name=".AddDepartmentActivity" />
        <activity android:name=".UpdDelCompanyActivity" />
        <activity android:name=".AddCompanyActivity" />
        <activity android:name=".AddOrderActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.DataBaseApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>